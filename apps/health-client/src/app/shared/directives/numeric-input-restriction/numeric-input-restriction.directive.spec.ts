import { NumericInputRestrictionDirective } from './numeric-input-restriction.directive';

describe('NumericInputRestrictionDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericInputRestrictionDirective();
    expect(directive).toBeTruthy();
  });
});
